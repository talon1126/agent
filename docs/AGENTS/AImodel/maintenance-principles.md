# 维护原则

- AImodel 是通用购物对话入口，不替代仓储、采购、物流、客服 agent 的部门职责。
- 商品推荐必须基于后端工具返回的真实商品；没有工具结果时，明确说明未找到合适商品。
- 前端 `AI模式` 入口只负责收集用户问题和商品链接，不在前端硬编码推荐结果。
- 新增或修改 AImodel 业务代码时，必须添加中文注释说明关键业务意图。
- 变更接口字段、工具返回结构、推荐链接规则或模型配置时，同步更新 `docs/AImodel/design.md` 和本目录文档。
- 不清楚的模型选择、工具边界、前端交互或接口字段，先向用户确认后再修改。
